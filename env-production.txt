# Production Environment Configuration for 100-200 Concurrent Users

# Database Configuration - Optimized for High Concurrency
DB_HOST=your_production_db_host
DB_PORT=5432
DB_NAME=election_enrollment
DB_USER=your_production_db_user
DB_PASSWORD=your_secure_production_password

# Twilio WhatsApp Configuration
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Application Configuration
NODE_ENV=production
PORT=3000

# Security (CRITICAL - Change these in production)
JWT_SECRET=your_super_secret_jwt_key_minimum_64_characters_for_production_security
JWT_EXPIRES_IN=24h
ENCRYPTION_KEY=your_encryption_key_for_sensitive_data_minimum_32_characters

# File Upload Configuration - Optimized for High Concurrency
MAX_FILE_SIZE=2097152
ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# Rate Limiting - Optimized for 100-200 Users
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50

# Database Connection Pool - Production Settings
DB_POOL_MAX=100
DB_POOL_MIN=20
DB_POOL_IDLE_TIMEOUT=10000
DB_POOL_CONNECTION_TIMEOUT=5000
DB_POOL_ACQUIRE_TIMEOUT=30000

# Performance Monitoring
ENABLE_MONITORING=true
LOG_LEVEL=info
ENABLE_QUERY_LOGGING=false

# Load Balancer Configuration
TRUST_PROXY=true
FORWARDED_FOR_HEADER=x-forwarded-for
